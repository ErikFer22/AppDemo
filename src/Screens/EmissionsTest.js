import { View, Text } from 'react-native'
import React from 'react'

// Native base imports
import { Button } from "native-base";

const EmissionsTest = ({ navigation }) => {
  return (
    
    <View style={{flex: 1}}>
      <Text>Desde el momento del reseteo del DTC:</Text>
      
    </View>
  )
}

export default EmissionsTest